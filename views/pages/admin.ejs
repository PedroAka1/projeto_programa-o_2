<%- include('../partials/head') %>
<%- include('../partials/navbar') %>

<div class="container mt-5">

  <h2>Dashboard</h2>
  <p class="text-muted">Bem-vindo, admin</p>

  <div class="row mt-4">

    <div class="col-md-4">
      <div class="card p-4 shadow-sm">
        <h5>Clientes</h5>
        <p class="text-muted">Gestão de clientes e contactos.</p>
        <span class="badge bg-light text-dark mb-3"><%= dados.totalClientes %></span>
        <a href="/clientes" class="btn btn-main">Abrir</a>
      </div>
    </div>

    <div class="col-md-4">
      <div class="card p-4 shadow-sm">
        <h5>Serviços</h5>
        <p class="text-muted">Reparações, upgrades e montagens.</p>
        <span class="badge bg-light text-dark mb-3"><%= dados.totalServicos %></span>
        <a href="/servicos" class="btn btn-main">Abrir</a>
      </div>
    </div>

    <div class="col-md-4">
      <div class="card p-4 shadow-sm">
        <h5>Produtos</h5>
        <p class="text-muted">Catálogo e stock de componentes.</p>
        <span class="badge bg-light text-dark mb-3"><%= dados.totalProdutos %></span>
        <a href="/produtos" class="btn btn-main">Abrir</a>
      </div>
    </div>

  </div>
</div>

<%- include('../partials/footer') %>